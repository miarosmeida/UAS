<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".UpdateActivity" android:orientation="vertical"
    android:gravity="center_vertical">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Update Data"
        android:textSize="20sp"
        android:textAlignment="center"
        android:gravity="center_horizontal"
        android:layout_margin="10dp"
        android:textStyle="bold"/>
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_idpu"
        android:hint="Id Produk!!"
        />
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_namau"
        android:hint="Nama Produk!!"/>
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_hargau"
        android:hint="Harga!!"/>
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_warnau"
        android:hint="Warna!!"/>
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_stoku"
        android:hint="Stok!!"/>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">
        <Button
            style="@style/btn_Style"
            android:id="@+id/btn_update"
            android:text="Update"
            android:onClick="updateData"/>
        <Button
            android:id="@+id/btn_cancel"
            style="@style/btn_Style"
            android:text="Cancel"
            android:onClick="cancelData"/>

    </LinearLayout>

</LinearLayout>